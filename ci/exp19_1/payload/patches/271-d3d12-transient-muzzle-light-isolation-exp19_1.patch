diff --git a/src/opengl/gl_d3d12raylight.cpp b/src/opengl/gl_d3d12raylight.cpp
index 08ffeb3..c784483 100644
--- a/src/opengl/gl_d3d12raylight.cpp
+++ b/src/opengl/gl_d3d12raylight.cpp
@@ -1828,6 +1828,10 @@ struct glRaytracingLightingState_t
 };
 
 static glRaytracingLightingState_t g_glRaytracingLighting;
+static uint32_t g_glRaytracingTransientReservedSlots = 0;
+static uint32_t g_glRaytracingTransientLightCount = 0;
+static uint32_t g_glRaytracingSelectedTransientLightCount = 0;
+static const float GL_RAYTRACING_LIGHT_FLAG_TRANSIENT = 0.25f;
 static const char g_glRaytracingLightingHlslPart0[] = R"(
 struct Light
 {
@@ -3625,9 +3629,16 @@ static void glRaytracingLightingUpdateConstants(void)
 		sizeof(g_glRaytracingLighting.constants));
 }
 
+static bool glRaytracingLightingIsTransient(const glRaytracingLight_t& light)
+{
+	return light.pad1 >= GL_RAYTRACING_LIGHT_FLAG_TRANSIENT && light.pad1 < GL_RAYTRACING_LIGHT_FLAG_FILL_ONLY;
+}
+
 static void glRaytracingLightingPrepareSelectedLights(void)
 {
 	g_glRaytracingLighting.selectedLights.clear();
+	g_glRaytracingTransientLightCount = 0;
+	g_glRaytracingSelectedTransientLightCount = 0;
 	if (g_glRaytracingLighting.cpuLights.empty())
 	{
 		g_glRaytracingLighting.constants.lightCount = 0;
@@ -3638,6 +3649,7 @@ static void glRaytracingLightingPrepareSelectedLights(void)
 	{
 		size_t index;
 		float score;
+		bool transient;
 	};
 
 	std::vector<ScoredLight> scored;
@@ -3649,6 +3661,9 @@ static void glRaytracingLightingPrepareSelectedLights(void)
 	for (size_t i = 0; i < g_glRaytracingLighting.cpuLights.size(); ++i)
 	{
 		const glRaytracingLight_t& light = g_glRaytracingLighting.cpuLights[i];
+		const bool isTransient = glRaytracingLightingIsTransient(light);
+		if (isTransient)
+			++g_glRaytracingTransientLightCount;
 		const float dx = light.position.x - cx;
 		const float dy = light.position.y - cy;
 		const float dz = light.position.z - cz;
@@ -3659,9 +3674,11 @@ static void glRaytracingLightingPrepareSelectedLights(void)
 		score += radius / (distance + 64.0f);
 		if (light.type == GL_RAYTRACING_LIGHT_TYPE_RECT)
 			score *= 1.20f;
-		if (light.pad1 >= 0.5f)
-			score += 1000000.0f; // keep only the explicit camera fill light
-		scored.push_back({ i, score });
+		if (isTransient)
+			score += 250000.0f; // keep muzzle/explosion candidates ahead of ordinary map lights
+		if (light.pad1 >= GL_RAYTRACING_LIGHT_FLAG_FILL_ONLY)
+			score += 1000000.0f; // preserve the explicit camera fill light
+		scored.push_back({ i, score, isTransient });
 	}
 
 	std::stable_sort(scored.begin(), scored.end(), [](const ScoredLight& a, const ScoredLight& b)
@@ -3671,10 +3688,29 @@ static void glRaytracingLightingPrepareSelectedLights(void)
 
 	const size_t limit = glRaytracingClamp<size_t>(
 		(size_t)g_glRaytracingPerformance.maxLights, 1, GL_RAYTRACING_MAX_LIGHTS);
-	const size_t selectedCount = (std::min)(limit, scored.size());
-	g_glRaytracingLighting.selectedLights.reserve(selectedCount);
-	for (size_t i = 0; i < selectedCount; ++i)
+	g_glRaytracingLighting.selectedLights.reserve((std::min)(limit, scored.size()));
+	std::vector<uint8_t> selected(scored.size(), 0);
+
+	const size_t reserve = (std::min)(
+		(size_t)g_glRaytracingTransientReservedSlots,
+		(std::min)(limit, (size_t)g_glRaytracingTransientLightCount));
+	for (size_t i = 0; i < scored.size() && g_glRaytracingSelectedTransientLightCount < reserve; ++i)
+	{
+		if (!scored[i].transient)
+			continue;
 		g_glRaytracingLighting.selectedLights.push_back(g_glRaytracingLighting.cpuLights[scored[i].index]);
+		selected[i] = 1;
+		++g_glRaytracingSelectedTransientLightCount;
+	}
+
+	for (size_t i = 0; i < scored.size() && g_glRaytracingLighting.selectedLights.size() < limit; ++i)
+	{
+		if (selected[i])
+			continue;
+		g_glRaytracingLighting.selectedLights.push_back(g_glRaytracingLighting.cpuLights[scored[i].index]);
+		if (scored[i].transient)
+			++g_glRaytracingSelectedTransientLightCount;
+	}
 
 	g_glRaytracingLighting.constants.lightCount = (uint32_t)g_glRaytracingLighting.selectedLights.size();
 	if (!g_glRaytracingLighting.selectedLights.empty())
@@ -4607,3 +4643,18 @@ uint32_t glRaytracingLightingGetSelectedLightCount(void)
 {
 	return (uint32_t)g_glRaytracingLighting.selectedLights.size();
 }
+
+void glRaytracingLightingSetTransientReservedSlots(uint32_t slots)
+{
+	g_glRaytracingTransientReservedSlots = (std::min)(slots, (uint32_t)GL_RAYTRACING_MAX_LIGHTS);
+}
+
+uint32_t glRaytracingLightingGetTransientLightCount(void)
+{
+	return g_glRaytracingTransientLightCount;
+}
+
+uint32_t glRaytracingLightingGetSelectedTransientLightCount(void)
+{
+	return g_glRaytracingSelectedTransientLightCount;
+}
diff --git a/src/opengl/opengl.h b/src/opengl/opengl.h
index a610e6e..5534101 100644
--- a/src/opengl/opengl.h
+++ b/src/opengl/opengl.h
@@ -1769,6 +1769,9 @@ glRaytracingLight_t        glRaytracingLightingMakeRectLight(
 
 uint32_t                   glRaytracingLightingGetLightCount(void);
 uint32_t                   glRaytracingLightingGetSelectedLightCount(void);
+void                       glRaytracingLightingSetTransientReservedSlots(uint32_t slots);
+uint32_t                   glRaytracingLightingGetTransientLightCount(void);
+uint32_t                   glRaytracingLightingGetSelectedTransientLightCount(void);
 
 void                       glLightScene(void);
 
diff --git a/src/renderer/tr_init.cpp b/src/renderer/tr_init.cpp
index 26870de..dd634f4 100644
--- a/src/renderer/tr_init.cpp
+++ b/src/renderer/tr_init.cpp
@@ -86,6 +86,14 @@ cvar_t  *r_dxrGameDynamicLightStrength;
 cvar_t  *r_dxrGameDynamicLightRadiusScale;
 cvar_t  *r_dxrGameDynamicLightShadows;
 cvar_t  *r_dxrGameDynamicLightDebug;
+cvar_t  *r_dxrGameLightFilterMode;
+cvar_t  *r_dxrTransientLightStrength;
+cvar_t  *r_dxrTransientLightRadiusScale;
+cvar_t  *r_dxrTransientLightMaxDistance;
+cvar_t  *r_dxrTransientLightMaxRadius;
+cvar_t  *r_dxrTransientLightLifetimeFrames;
+cvar_t  *r_dxrTransientLightReservedSlots;
+cvar_t  *r_dxrTransientLightShadows;
 cvar_t  *r_dxrMaterialSpecularMode;
 cvar_t  *r_dxrToneMappingMode;
 cvar_t  *r_dxrToneMapWhitePoint;
@@ -1051,6 +1059,14 @@ void R_Register( void ) {
 	r_dxrGameDynamicLightRadiusScale = ri.Cvar_Get( "r_dxrGameDynamicLightRadiusScale", "1.0", CVAR_ARCHIVE );
 	r_dxrGameDynamicLightShadows = ri.Cvar_Get( "r_dxrGameDynamicLightShadows", "1", CVAR_ARCHIVE );
 	r_dxrGameDynamicLightDebug = ri.Cvar_Get( "r_dxrGameDynamicLightDebug", "0", CVAR_ARCHIVE );
+	r_dxrGameLightFilterMode = ri.Cvar_Get( "r_dxrGameLightFilterMode", "0", CVAR_ARCHIVE );
+	r_dxrTransientLightStrength = ri.Cvar_Get( "r_dxrTransientLightStrength", "1.0", CVAR_ARCHIVE );
+	r_dxrTransientLightRadiusScale = ri.Cvar_Get( "r_dxrTransientLightRadiusScale", "1.0", CVAR_ARCHIVE );
+	r_dxrTransientLightMaxDistance = ri.Cvar_Get( "r_dxrTransientLightMaxDistance", "224.0", CVAR_ARCHIVE );
+	r_dxrTransientLightMaxRadius = ri.Cvar_Get( "r_dxrTransientLightMaxRadius", "512.0", CVAR_ARCHIVE );
+	r_dxrTransientLightLifetimeFrames = ri.Cvar_Get( "r_dxrTransientLightLifetimeFrames", "10", CVAR_ARCHIVE );
+	r_dxrTransientLightReservedSlots = ri.Cvar_Get( "r_dxrTransientLightReservedSlots", "2", CVAR_ARCHIVE );
+	r_dxrTransientLightShadows = ri.Cvar_Get( "r_dxrTransientLightShadows", "0", CVAR_ARCHIVE );
 	r_dxrMaterialSpecularMode = ri.Cvar_Get( "r_dxrMaterialSpecularMode", "0", CVAR_ARCHIVE );
 	r_dxrToneMappingMode = ri.Cvar_Get( "r_dxrToneMappingMode", "0", CVAR_ARCHIVE );
 	r_dxrToneMapWhitePoint = ri.Cvar_Get( "r_dxrToneMapWhitePoint", "4.0", CVAR_ARCHIVE );
diff --git a/src/renderer/tr_local.h b/src/renderer/tr_local.h
index c2801e7..044b50e 100644
--- a/src/renderer/tr_local.h
+++ b/src/renderer/tr_local.h
@@ -1144,6 +1144,14 @@ extern cvar_t   *r_dxrGameDynamicLightStrength; // intensity multiplier for subm
 extern cvar_t   *r_dxrGameDynamicLightRadiusScale; // radius multiplier for submitted game dlights
 extern cvar_t   *r_dxrGameDynamicLightShadows; // point-shadow rays for game dlights
 extern cvar_t   *r_dxrGameDynamicLightDebug; // one-second bridge diagnostics
+extern cvar_t   *r_dxrGameLightFilterMode; // 0 all, 1 persistent only, 2 transient only
+extern cvar_t   *r_dxrTransientLightStrength; // additional multiplier for newly appearing near-camera lights
+extern cvar_t   *r_dxrTransientLightRadiusScale; // additional radius scale for transient candidates
+extern cvar_t   *r_dxrTransientLightMaxDistance; // camera-distance gate for transient candidates
+extern cvar_t   *r_dxrTransientLightMaxRadius; // source-radius gate for transient candidates
+extern cvar_t   *r_dxrTransientLightLifetimeFrames; // maximum observed age for transient classification
+extern cvar_t   *r_dxrTransientLightReservedSlots; // slots reserved during DXR importance selection
+extern cvar_t   *r_dxrTransientLightShadows; // point-shadow rays for transient candidates
 extern cvar_t   *r_dxrMaterialSpecularMode; // 0 legacy, 1 roughness only, 2 material F0, 3 characters matte, 4 world matte, 5 balanced, 6 strict
 extern cvar_t   *r_dxrToneMappingMode; // 0 legacy clamp, 1 Reinhard, 2 ACES, 3 Hable, 4 neutral, 5 balanced HDR, 6 cinematic HDR
 extern cvar_t   *r_dxrToneMapWhitePoint; // highlight shoulder reference point
